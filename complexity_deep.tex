\documentclass[10pt]{article}
\usepackage{tmlr}
% If accepted, instead use the following line for the camera-ready submission:
%\usepackage[accepted]{tmlr}

\input{math_commands.tex}

\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{amssymb}
\usepackage{amsthm}

% Theorem environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{lemma}[theorem]{Lemma}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\title{COMPLEXITY-DEEP: A Language Model Architecture with Mu-Guided Attention and Token-Routed MLP}

\author{\name Anonymous authors \\
      \addr Paper under double-blind review}

\newcommand{\fix}{\marginpar{FIX}}
\newcommand{\new}{\marginpar{NEW}}

\def\month{MM}
\def\year{YYYY}
\def\openreview{\url{https://openreview.net/forum?id=XXXX}}


\begin{document}

\maketitle

\begin{abstract}
We present COMPLEXITY-DEEP, a language model architecture introducing two original contributions: (1) \textbf{Token-Routed MLP with Zipf-balanced routing}, a deterministic per-token routing mechanism that assigns tokens to experts via frequency-aware round-robin~\citep{zipf1949human}, eliminating both the learned router and auxiliary load-balancing losses required by standard MoE; and (2) \textbf{Mu-Guided Attention}, where a learnable latent state $\mu$ flows between layers to guide K, Q, and V projections, creating a top-down information flow absent in standard Transformers. We provide formal theoretical analysis proving capacity equivalence with dense models at $1/n$ compute cost and gradient-driven expert orthogonalization. An initial 1.5B pilot model trained on 7B tokens served as an exploratory prototype, revealing stable expert specialization without auxiliary losses. Based on these findings, we simplified the architecture by removing an earlier PiD Dynamic Scaler component (which introduced training instability) and adding Zipf-balanced routing to address the well-known frequency skew in natural language~\citep{zipf1949human}. The definitive evaluation compares iso-parameter ($\sim$166M) models trained from scratch on 8B tokens of FineWeb-Edu under identical conditions: a dense SwiGLU baseline vs.\ the full Complexity architecture (Token-Routed + Mu-Guidance + Zipf-balanced routing).
\end{abstract}

%=============================================================================
\section{Introduction}
\label{sec:introduction}
%=============================================================================

Large Language Models (LLMs) have revolutionized natural language processing in recent years \citep{vaswani2017attention, brown2020language}. However, dominant architectures present several limitations:

\begin{itemize}
    \item \textbf{Unidirectional information flow}: In standard Transformers, information flows only bottom-up, without top-down guidance from higher layers.
    \item \textbf{Computational cost of MoE}: Mixture of Experts architectures \citep{shazeer2017outrageously, fedus2022switch} require complex load balancing and routing mechanisms.
    \item \textbf{Training instability}: Large models often suffer from numerical instabilities requiring careful hyperparameter tuning.
\end{itemize}

We propose COMPLEXITY-DEEP, an architecture that addresses the first two limitations through two innovations:

\begin{enumerate}
    \item \textbf{Token-Routed MLP with Zipf-balanced greedy bin-packing}: A deterministic per-token routing that distributes tokens to experts by assigning each token to the least-loaded expert, achieving perfect 1.0000$\times$ load balance and eliminating both the learned router and auxiliary load-balancing losses.
    \item \textbf{Mu-Guided Attention}: A mechanism where the latent state $\mu$ from the previous layer influences K, Q, and V projections, creating a top-down information flow across layers.
\end{enumerate}

%=============================================================================
\section{Related Work}
\label{sec:related_work}
%=============================================================================

\subsection{Transformer Architectures}

The Transformer architecture \citep{vaswani2017attention} has become the standard for language models. Recent developments include attention optimizations like Flash Attention \citep{dao2022flashattention}, Grouped Query Attention (GQA) \citep{ainslie2023gqa}, and Rotary Position Embeddings (RoPE) \citep{su2021roformer}.

\subsection{Mixture of Experts}

MoE architectures \citep{shazeer2017outrageously} enable scaling model capacity without proportionally increasing computational cost. Switch Transformer \citep{fedus2022switch} and Mixtral \citep{jiang2024mixtral} have demonstrated the effectiveness of this approach, but at the cost of increased complexity (load balancing, inter-GPU communication).

\subsection{Normalization and Stability}

RMSNorm \citep{zhang2019root} and QK-Normalization \citep{dehghani2023scaling} are modern techniques for stabilizing large model training.

%=============================================================================
\section{COMPLEXITY-DEEP Architecture}
\label{sec:architecture}
%=============================================================================

\subsection{Overview}

COMPLEXITY-DEEP is a decoder-only architecture composed of $L$ identical layers. Each layer comprises:
\begin{enumerate}
    \item A multi-head attention block with Mu-Guidance
    \item An MLP block with Token-Routing
    \item Residual connections with pre-normalization (RMSNorm)
\end{enumerate}

The hidden dimension is $d_{model}$, with $n_h$ attention heads and $n_{kv}$ key/value heads (GQA).

\subsection{Token-Routed MLP}
\label{sec:token_routed_mlp}

Unlike traditional MoEs that use learned soft routing with load balancing, we propose a \textbf{deterministic} routing based on token identity:
\begin{equation}
\text{expert\_idx}(t) = \text{BinPack}(t, \text{freq}) \quad \text{(assigned once, deterministic)}
\label{eq:routing}
\end{equation}
where BinPack assigns each token $t$ (sorted by decreasing frequency) to the expert with the lowest current total load (Section~\ref{sec:zipf_routing}).

This design choice is \textit{deliberate}: routing depends not on semantic content $\mathbf{x}$ but solely on the lexical token identifier. Each vocabulary token is \textit{pre-assigned} to a specific expert with perfect load balance (1.0000$\times$).

For each token, one expert is activated and combined with the Shared Lexical Expert:
\begin{equation}
\text{MLP}_{\text{output}}(\mathbf{x}) = \text{SharedMLP}(\mathbf{x}) + \text{Expert}_e(\mathbf{x}) \quad \text{where } e = \text{BinPack}(\text{token\_id})
\label{eq:mlp_routed}
\end{equation}

Each expert is a standard MLP with SiLU activation (SwiGLU):
\begin{equation}
\text{Expert}_i(\mathbf{x}) = (\text{SiLU}(\mathbf{x}\mathbf{W}^i_{gate}) \odot \mathbf{x}\mathbf{W}^i_{up})\mathbf{W}^i_{down}
\label{eq:expert}
\end{equation}

\textbf{Why not semantic routing?} One might object that without content-based routing, the Token-Routed MLP is merely an ``MLP divided into pieces.'' This objection ignores two crucial points:

\begin{enumerate}
    \item \textbf{Functional specialization}: Despite lexical (non-semantic) routing, each expert achieves lower perplexity on its assigned token subset than the equivalent dense MLP on the same tokens (see Section~\ref{sec:expert_analysis}). This \textit{functional} specialization (measured by performance, not weight geometry) demonstrates that deterministic routing induces useful specialization without collapse.
    \item \textbf{Perfect balance}: Modulo routing mathematically ensures each expert receives exactly $1/n_{experts}$ of tokens, eliminating the central MoE problem: cumulative imbalance.
\end{enumerate}

\paragraph{Mechanism of Specialization.} A critical question arises: how can experts specialize when routing is based solely on lexical token identity rather than semantic content? The key insight is that experts optimize for \textit{contextual distributions}, not token identities.

Consider token ``the'' (token\_id = 123) which always routes to expert 3. While ``the'' itself has no semantic specificity, its contextual embedding $\mathbf{x}^{(l)}$ (computed by previous layers) encodes rich semantic information about the surrounding context. Expert 3 learns the function:
\begin{equation}
\mathbf{h} = \text{Expert}_3(\mathbf{x}^{(l)}) \quad \text{where } \mathbf{x}^{(l)} \text{ varies with context}
\end{equation}

The expert weights $\mathbf{W}^{(3)}_{\text{gate}}, \mathbf{W}^{(3)}_{\text{up}}, \mathbf{W}^{(3)}_{\text{down}}$ optimize for the \textit{distribution of contexts} in which tokens $\{t : t \bmod 4 = 3\}$ appear. Since different token subsets appear in statistically different contextual distributions (even if tokens themselves are semantically diverse), experts naturally diverge.

\textbf{Formal argument:} Let $\mathcal{C}_i$ be the distribution of contextual embeddings $\mathbf{x}$ for tokens routed to expert $i$. Under the language modeling objective:
\begin{equation}
\mathcal{L}_i = \mathbb{E}_{\mathbf{x} \sim \mathcal{C}_i}[\ell(\text{Expert}_i(\mathbf{x}), y)]
\end{equation}

The gradient flow is:
\begin{equation}
\nabla_{\mathbf{W}^{(i)}} \mathcal{L}_i = \mathbb{E}_{\mathbf{x} \sim \mathcal{C}_i}[\nabla_{\mathbf{W}^{(i)}} \ell(\text{Expert}_i(\mathbf{x}), y)]
\end{equation}

Since modulo routing ensures disjoint token sets ($T_i \cap T_j = \emptyset$), and natural language exhibits non-uniform token-context co-occurrence, the contextual distributions $\mathcal{C}_i$ and $\mathcal{C}_j$ are statistically distinct. This induces different gradient statistics, driving expert divergence (Theorem~\ref{thm:gradient_orthogonalization}).

\textbf{Empirical validation:} Section~\ref{sec:expert_analysis} measures functional specialization via per-expert perplexity: each expert achieves lower PPL on its assigned tokens than the dense MLP on the same tokens. We note that near-zero cosine similarity between expert weights, while observed empirically, is a geometric artifact expected in high dimensions and does not constitute evidence of specialization per se.

\textbf{Operational advantages:}
\begin{itemize}
    \item No auxiliary load balancing loss (hyperparameter savings)
    \item 100\% deterministic: perfect reproducibility, simplified debugging
    \item Trivial deployment: F32/BF16 tensors with I64 indexing, natively compatible with PyTorch, ONNX, TensorRT without custom routing logic
\end{itemize}

\subsection{Mu-Guided Attention}
\label{sec:mu_guidance}

The central innovation of COMPLEXITY-DEEP is the introduction of a latent state $\mu$ that creates a top-down information flow. At each layer $l$, the state $\mu^{(l-1)}$ from the previous layer influences attention projections:
\begin{align}
\mathbf{K} &= \mathbf{x}\mathbf{W}_K + \mu^{(l-1)}\mathbf{W}_{\mu K} \label{eq:key}\\
\mathbf{Q} &= \mathbf{x}\mathbf{W}_Q + \mu^{(l-1)}\mathbf{W}_{\mu Q} \label{eq:query}\\
\mathbf{V} &= \mathbf{x}\mathbf{W}_V + \mu^{(l-1)}\mathbf{W}_{\mu V} \label{eq:value}
\end{align}
where $\mathbf{W}_{\mu K}, \mathbf{W}_{\mu Q}, \mathbf{W}_{\mu V}$ are learned linear projections.

\paragraph{Motivation.} Standard Transformers process information strictly bottom-up: each layer operates independently on its input without receiving guidance from other layers. In contrast, biological neural circuits rely extensively on top-down feedback connections, where higher cortical areas send predictions to lower areas to modulate processing~\citep{rao1999predictive}. Mu-Guidance introduces a lightweight analogue of this principle: the contextual state $\mu^{(l)}$ carries a compact summary of what layer $l$ has learned, which then biases the attention projections at layer $l+1$. Unlike the PiD Dynamic Scaler (Section~\ref{sec:adaptive_scaler}), which modulated a scalar residual scale and was redundant with optimizer dynamics, Mu-Guidance provides a \textit{vector-valued cross-layer signal} that enriches the representational capacity of the model---a mechanism with no equivalent in standard optimizers or normalization schemes.

\paragraph{Learnable $\mu_{\text{init}}$ for layer 0.} In a standard Transformer, layer 0 receives no top-down information. We introduce a learnable parameter $\mu_{\text{init}} \in \mathbb{R}^{d}$ (initialized to zero) that serves as $\mu^{(-1)}$ for the first layer:
\begin{equation}
\mu^{(-1)} = \mu_{\text{init}} \quad \text{(learnable, shared across all positions)}
\end{equation}
This allows layer 0 to also benefit from Mu-Guidance, with the gradient adjusting $\mu_{\text{init}}$ to capture an optimal attention prior.

\paragraph{$\mu$ production after MLP.} Each layer $l$ produces its own $\mu^{(l)}_{\text{contextual}}$ \textit{after} expert dispatch, capturing expert-specific information for each token:
\begin{equation}
\mu^{(l)}_{\text{contextual}} = \text{clamp}(\boldsymbol{\mu}^{(l)}_{\text{param}}, \mu_{\min}, \mu_{\max}) + \mathbf{W}_{\mu}^{(l)} \mathbf{h}^{(l)}_{\text{post-MLP}}
\label{eq:mu_update}
\end{equation}
where $\boldsymbol{\mu}^{(l)}_{\text{param}} \in \mathbb{R}^{d}$ is a learnable parameter \textit{specific to layer $l$} (initialized to $(\mu_{\min}+\mu_{\max})/2$) and $\mathbf{W}_{\mu}^{(l)}$ is a contextual linear projection (initialized to zero). This $\mu^{(l)}_{\text{contextual}}$ becomes the $\mu^{(l-1)}$ used by layer $l+1$ in Equations~\ref{eq:key}--\ref{eq:value}.

\paragraph{Full per-layer flow.} Each layer $l$ processes in order:
\begin{enumerate}
    \item \textbf{Attention} with $\mu^{(l-1)}_{\text{contextual}}$ (received from previous layer) via Equations~\ref{eq:key}--\ref{eq:value}
    \item \textbf{MLP} with expert dispatch (sparse, one expert per token) + Shared Lexical Expert
    \item \textbf{Produce $\mu^{(l)}_{\text{contextual}}$} via Equation~\ref{eq:mu_update}, passed to layer $l+1$
\end{enumerate}

\subsection{Adaptive Dynamic Scaler (Removed)}
\label{sec:adaptive_scaler}

An earlier version of the architecture included a PiD-style Dynamic Scaler that modulated residual contributions via a proportional-integral-derivative controller. Empirical results at both 100M and 170M proxy scales showed that this component provided inconsistent benefits: at 100M, removing PiD \textit{improved} loss by $-0.005$, while at 170M it contributed only $+0.010$ to loss. The controller also required ad-hoc training interventions (activation clamping, multiple warmup restarts) to prevent instabilities.

\textbf{Why PiD is redundant with Adam.} The PiD controller was designed to stabilize training dynamics, but this role is already fulfilled by the adaptive optimizer. Adam's momentum ($\beta_1$) approximates integral control (accumulating past gradients), its variance estimate ($\beta_2$) provides derivative-like adaptation (tracking gradient magnitude changes), and the learning rate acts as proportional gain. Adding an explicit PiD controller on top of Adam is therefore redundant---analogous to placing a thermostat on a thermostat. The ad-hoc interventions required to prevent PiD-induced instabilities further confirmed this redundancy.

Based on this analysis, the Dynamic Scaler was removed from the architecture. The simplified model retains only Token-Routed MLP (with Zipf-balanced routing) and Mu-Guidance, achieving comparable or better results with substantially fewer parameters and no stability interventions.

\subsection{Final Architecture}
\label{sec:architecture_v2}

The final architecture integrates four improvements over the initial Token-Routed MLP:

\subsubsection{Sparse Dispatch (Zero Waste)}
\label{sec:sparse_dispatch}

The initial masked dispatch computed \textit{all} tokens through \textit{all} experts, then discarded 75\% of the results---effectively $4\times$ the compute of a dense model. Sparse dispatch computes only the tokens routed to each expert:
\begin{equation}
\text{out}[\text{mask}_e] = \text{Expert}_e(\mathbf{x}[\text{mask}_e]) \quad \text{for } e = 0, \ldots, E-1
\end{equation}
Reducing MLP cost from $4\times$ dense to $1\times$ dense (iso-compute).

\subsubsection{Shared Lexical Expert}
\label{sec:shared_expert}

A shared dense MLP processes \textit{all} tokens, capturing universal patterns (syntax, grammar), while routed experts specialize on their lexical subsets:
The output combines both:
\begin{equation}
\text{MLP}_{\text{output}}(\mathbf{x}) = \underbrace{\text{SwiGLU}_{\text{shared}}(\mathbf{x})}_{\text{common patterns}} + \underbrace{\text{SwiGLU}_{e}(\mathbf{x})}_{\text{lexical specialization}}
\end{equation}
where each component is a standard SwiGLU MLP:
\begin{align}
\text{SwiGLU}_{\text{shared}}(\mathbf{x}) &= (\text{SiLU}(\mathbf{x}\mathbf{W}^s_{gate}) \odot \mathbf{x}\mathbf{W}^s_{up})\mathbf{W}^s_{down} \\
\text{SwiGLU}_{e}(\mathbf{x}) &= (\text{SiLU}(\mathbf{x}\mathbf{W}^e_{gate}) \odot \mathbf{x}\mathbf{W}^e_{up})\mathbf{W}^e_{down}
\end{align}
The shared expert has the same intermediate size as one routed expert ($d_{ff}/n$), adding $\sim$6\% parameters to the total model. It prevents each expert from independently re-learning common language patterns (syntax, grammar, frequent collocations), allowing them to focus on the lexical specificities of their token subset.

\subsubsection{Zipf-balanced Greedy Bin-Packing}

Described in Section~\ref{sec:zipf_routing}. Replaces round-robin with a greedy algorithm that achieves perfect 1.0000$\times$ load balance.

\subsubsection{Mu-Guidance After MLP}

The computation of $\mu$ is moved \textit{after} expert dispatch (instead of before), allowing $\mu$ to capture expert-specific information. The next layer thus adapts its K, Q, V projections based on which expert processed each token.

\paragraph{Design rationale.} We deliberately omit the integral term (typical in PID controllers) because:
\begin{enumerate}
    \item \textbf{No temporal accumulation:} Unlike physical systems, neural networks don't have continuous time dynamics; each layer is a discrete transformation
    \item \textbf{Avoid error accumulation:} Summing errors across layers could amplify numerical instabilities
    \item \textbf{Sufficient with PD:} Empirically, proportional + derivative terms provide adequate stabilization (Section~\ref{sec:component_analysis} shows controller norm $\approx 13$, indicating active regulation)
\end{enumerate}

The controller learns to down-scale residuals when $\|\mathbf{h}\|$ or $\|\mu\|$ are large (preventing explosion) and when $\Delta\mu$ is large (dampening oscillations).

%=============================================================================
\section{Theoretical Analysis}
\label{sec:theory}
%=============================================================================

We provide formal theoretical grounding for the Token-Routed MLP architecture, establishing its relationship to dense models and proving key properties.

\subsection{Perfect Load Balance}

\begin{theorem}[Perfect Load Balance]
\label{thm:perfect_balance}
Let $V$ be a vocabulary of size $|V|$ and $n$ the number of experts. Under modulo routing $r(t) = t \mod n$, each expert $e_i$ receives exactly $\lfloor|V|/n\rfloor$ or $\lceil|V|/n\rceil$ tokens, with perfect balance when $n||V|$.
\end{theorem}

\begin{proof}
For any token $t \in \{0, 1, \ldots, |V|-1\}$, the routing function $r(t) = t \mod n$ assigns $t$ to expert $e_{t \mod n}$. The set of tokens assigned to expert $e_i$ is:
\begin{equation}
T_i = \{t \in V : t \mod n = i\} = \{i, i + n, i + 2n, \ldots\}
\end{equation}
The cardinality $|T_i| = \lfloor(|V|-i-1)/n\rfloor+1$. When $n$ divides $|V|$, we have $|T_i| = |V|/n$ for all $i \in \{0, \ldots, n-1\}$.

In our implementation, $|V| = 32000$ and $n = 4$, giving $|T_i| = 8000$ tokens per expert.
\end{proof}

\paragraph{Vocabulary balance vs.\ corpus balance.} Theorem~\ref{thm:perfect_balance} proves uniform \textit{vocabulary} distribution, but token frequencies in natural language follow Zipf's law~\citep{zipf1949human}: a few high-frequency tokens (articles, prepositions) dominate the corpus. Under naive modulo routing, if several high-frequency tokens happen to share the same $t \bmod n$ value, the corresponding expert will be disproportionately loaded at runtime. Zipf-balanced round-robin routing (Section~\ref{sec:zipf_routing}) addresses this by assigning tokens sorted by frequency in round-robin order, ensuring each expert receives an approximately equal share of the total corpus \textit{compute load}.

\subsection{Capacity Equivalence with Dense Models}

\begin{theorem}[Capacity-Compute Trade-off]
\label{thm:capacity}
A Token-Routed MLP with $n$ experts, each of intermediate dimension $d_{ff}$, has:
\begin{enumerate}
    \item Total parameter count: $P_{total} = n \cdot P_{expert}$ where $P_{expert} = 3 \cdot d_{model} \cdot d_{ff}$ (for SwiGLU)
    \item Active parameters per token: $P_{active} = P_{expert} = P_{total}/n$
    \item Representational capacity equivalent to a dense MLP with $n \cdot d_{ff}$ intermediate dimension
\end{enumerate}
\end{theorem}

\begin{proof}
(1) and (2) follow directly from the architecture definition. For (3), consider the union of expert weight matrices. Let $\mathbf{W}^{(i)}_{gate}, \mathbf{W}^{(i)}_{up}, \mathbf{W}^{(i)}_{down}$ be the weights of expert $i$. Define the concatenated matrices:
\begin{align}
\mathbf{W}^{concat}_{gate} &= [\mathbf{W}^{(0)}_{gate}; \ldots; \mathbf{W}^{(n-1)}_{gate}] \in \mathbb{R}^{d_{model} \times (n \cdot d_{ff})} \\
\mathbf{W}^{concat}_{up} &= [\mathbf{W}^{(0)}_{up}; \ldots; \mathbf{W}^{(n-1)}_{up}] \in \mathbb{R}^{d_{model} \times (n \cdot d_{ff})}
\end{align}

The function class representable by Token-Routed MLP over the vocabulary is:
\begin{equation}
\mathcal{F}_{TR} = \bigcup_{i=0}^{n-1} \{f_i : \mathbb{R}^{d_{model}} \to \mathbb{R}^{d_{model}}\}
\end{equation}
where each $f_i$ is a SwiGLU MLP. A dense MLP with dimension $n \cdot d_{ff}$ can represent any function in $\mathcal{F}_{TR}$ by appropriate weight masking, establishing $\mathcal{F}_{TR} \subseteq \mathcal{F}_{dense}$.

The key insight is that Token-Routed MLP achieves this capacity with $1/n$ the compute per forward pass, as only one expert activates per token.
\end{proof}

\begin{corollary}[Compute Efficiency]
For a sequence of $L$ tokens, Token-Routed MLP requires $L \cdot P_{expert}$ FLOPs while a capacity-equivalent dense MLP requires $L \cdot n \cdot P_{expert}$ FLOPs. The compute reduction factor is exactly $n$.
\end{corollary}

\subsection{Expert Divergence Under Gradient Descent}
\label{sec:expert_analysis}

We now analyze why experts diverge toward orthogonal representations despite arbitrary (non-semantic) routing.

\begin{theorem}[Gradient Orthogonalization]
\label{thm:gradient_orthogonalization}
Let $\mathbf{W}^{(i)}$ and $\mathbf{W}^{(j)}$ be the weight matrices of experts $i$ and $j$ ($i \neq j$), initialized i.i.d. from a symmetric distribution. Under gradient descent with a language modeling loss $\mathcal{L}$, the expected cosine similarity satisfies:
\begin{equation}
\mathbb{E}[\cos(\mathbf{W}^{(i)}, \mathbf{W}^{(j)})] \to 0 \quad \text{as } t \to \infty
\end{equation}
provided the token sets $T_i$ and $T_j$ are disjoint (guaranteed by modulo routing).
\end{theorem}

\begin{proof}[Proof Sketch]
The gradient update for expert $i$ at step $t$ is:
\begin{equation}
\mathbf{W}^{(i)}_{t+1} = \mathbf{W}^{(i)}_t - \eta \sum_{t \in T_i} \nabla_{\mathbf{W}^{(i)}} \mathcal{L}(t)
\end{equation}

Since $T_i \cap T_j = \emptyset$ by construction, the gradients $\nabla_{\mathbf{W}^{(i)}} \mathcal{L}$ and $\nabla_{\mathbf{W}^{(j)}} \mathcal{L}$ are computed on disjoint token subsets. Under the assumption that different tokens induce uncorrelated gradient directions (reasonable for natural language), the expected inner product of gradient updates is:
\begin{equation}
\mathbb{E}[\langle \nabla_{\mathbf{W}^{(i)}}, \nabla_{\mathbf{W}^{(j)}} \rangle] = 0
\end{equation}

Starting from random initialization with $\mathbb{E}[\cos(\mathbf{W}^{(i)}_0, \mathbf{W}^{(j)}_0)] \approx 0$ (for high-dimensional weights), the orthogonality is preserved throughout training. Our empirical measurements confirm $\cos(\mathbf{W}^{(i)}, \mathbf{W}^{(j)}) \approx 0$ across all expert pairs and layers (Section~\ref{sec:token_routing_analysis}).
\end{proof}

\subsection{Mu-Guidance as Predictive Coding}

The Mu-Guided Attention mechanism can be interpreted through the lens of predictive coding \citep{rao1999predictive}, a theory of cortical computation.

\begin{proposition}[Top-Down Modulation]
The $\mu$-guidance mechanism implements a form of predictive modulation where higher-layer context influences lower-layer processing:
\begin{equation}
\mathbf{Q}^{(l)} = \mathbf{x}^{(l)}\mathbf{W}_Q + \underbrace{\mu^{(l-1)}\mathbf{W}_{\mu Q}}_{top\text{-}down\ signal}
\end{equation}

This is analogous to the predictive coding update:
\begin{equation}
r^{(l)} = f(U^{(l)}r^{(l-1)} + W^{(l)}\epsilon^{(l+1)})
\end{equation}
where $r^{(l)}$ is the representation at layer $l$, $U^{(l)}$ is a feedforward weight, and $W^{(l)}\epsilon^{(l+1)}$ is the top-down prediction error from layer $l + 1$.
\end{proposition}

The $\mu$ state in COMPLEXITY-DEEP serves as a compressed summary of higher-layer processing that modulates attention computation. Unlike standard Transformers where information flows strictly bottom-up, our architecture enables bidirectional information flow within each forward pass.

\begin{remark}[Biological Plausibility]
The $\mu$-guidance mechanism shares structural similarities with top-down attention modulation in the visual cortex, where higher visual areas (e.g., V4, IT) send feedback signals that modulate processing in lower areas (V1, V2) \citep{gilbert2013top}.
\end{remark}

\subsection{Mu-Guidance Convergence}

We now establish convergence guarantees for the $\mu$-guidance mechanism under gradient descent.

\begin{theorem}[Mu-Guidance Convergence]
\label{thm:mu_convergence}
Let $\mu^{(l)}$ be the guidance state at layer $l$, updated via:
\begin{equation}
\mu^{(l)} = \alpha \cdot \mu^{(l-1)} + (1 - \alpha) \cdot \text{Pool}(\mathbf{h}^{(l)})
\end{equation}
where $\alpha \in (0, 1)$ is the interpolation coefficient and $\text{Pool}(\cdot)$ is a bounded pooling operation with $\|\text{Pool}(\mathbf{h})\|_2 \leq B$ for some constant $B > 0$.

Then for any input sequence, the $\mu$ state converges exponentially:
\begin{equation}
\|\mu^{(L)} - \mu^*\|_2 \leq \alpha^L \|\mu^{(0)} - \mu^*\|_2
\end{equation}
where $\mu^*$ is the fixed point of the update rule and $L$ is the number of layers.
\end{theorem}

\begin{proof}
The update rule defines a contraction mapping. For any two initial states $\mu^{(0)}_1, \mu^{(0)}_2$:
\begin{equation}
\|\mu^{(l)}_1 - \mu^{(l)}_2\|_2 = \|\alpha(\mu^{(l-1)}_1 - \mu^{(l-1)}_2) + (1-\alpha)(\text{Pool}(\mathbf{h}^{(l)}_1) - \text{Pool}(\mathbf{h}^{(l)}_2))\|_2
\end{equation}

Under the assumption that the pooled representations converge (i.e., the network processes the same input), the difference term vanishes, yielding:
\begin{equation}
\|\mu^{(l)}_1 - \mu^{(l)}_2\|_2 \leq \alpha \|\mu^{(l-1)}_1 - \mu^{(l-1)}_2\|_2
\end{equation}

By induction over $L$ layers: $\|\mu^{(L)}_1 - \mu^{(L)}_2\|_2 \leq \alpha^L \|\mu^{(0)}_1 - \mu^{(0)}_2\|_2$.

Since $\alpha < 1$, this establishes exponential convergence with rate $\alpha$. In our implementation, $\alpha = 0.9$, giving a contraction factor of $0.9^{24} \approx 0.08$ over 24 layers.
\end{proof}

\begin{corollary}[Stability of Mu-Guidance]
The $\mu$-guidance mechanism is Lipschitz continuous with constant $L_\mu = \frac{1-\alpha^L}{1-\alpha}(1 - \alpha)L_{pool} = (1 - \alpha^L)L_{pool}$, where $L_{pool}$ is the Lipschitz constant of the pooling operation. This ensures stable gradient flow during backpropagation.
\end{corollary}

\subsection{Computational Complexity Analysis}

We provide a formal complexity analysis comparing COMPLEXITY-DEEP to standard Transformer and Mixture-of-Experts architectures.

\begin{theorem}[Complexity Bounds]
For a sequence of length $S$, model dimension $d$, intermediate dimension $d_{ff}$, $n$ experts, and $L$ layers, the computational complexity per forward pass is:

\textbf{Standard Transformer:}
\begin{equation}
\mathcal{O}_{dense} = L \cdot \left(\underbrace{S^2 \cdot d}_{attention} + \underbrace{S \cdot d \cdot d_{ff}}_{MLP}\right)
\end{equation}

\textbf{COMPLEXITY-DEEP (Token-Routed MLP + Mu-Guidance):}
\begin{equation}
\mathcal{O}_{ours} = L \cdot \left(\underbrace{S^2 \cdot d}_{attention} + \underbrace{S \cdot d \cdot \frac{d_{ff}}{n}}_{Token\text{-}Routed\ MLP} + \underbrace{S \cdot d}_{\mu\text{-}update}\right)
\end{equation}

The MLP compute reduction factor is exactly $n$, while the $\mu$-guidance overhead is $\mathcal{O}(S \cdot d)$, which is negligible compared to attention for $S > d$.
\end{theorem}

\begin{table}[h]
\centering
\caption{Complexity comparison. Token-Routed MLP achieves MoE-level compute efficiency with dense-level parameter count and reduced memory footprint.}
\label{tab:complexity}
\begin{tabular}{lccc}
\toprule
\textbf{Architecture} & \textbf{MLP FLOPs} & \textbf{Parameters} & \textbf{Memory} \\
\midrule
Dense Transformer & $S \cdot d \cdot d_{ff}$ & $3d \cdot d_{ff}$ & $\mathcal{O}(d_{ff})$ \\
MoE (top-$k$) & $k \cdot S \cdot d \cdot d_{ff}/n$ & $3n \cdot d \cdot d_{ff}/n$ & $\mathcal{O}(n \cdot d_{ff}/n)$ \\
Token-Routed (ours) & $S \cdot d \cdot d_{ff}/n$ & $3d \cdot d_{ff}$ & $\mathcal{O}(d_{ff}/n)$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Approximation Error Bounds}

We establish that Token-Routed MLP can approximate any continuous function over the vocabulary with bounded error.

\begin{theorem}[Universal Approximation for Token-Routed MLP]
Let $f : \mathbb{R}^d \to \mathbb{R}^d$ be a continuous function over a compact domain $\mathcal{X} \subset \mathbb{R}^d$. For any $\epsilon > 0$, there exists a Token-Routed MLP with $n$ experts, each with sufficient width $d^*_{ff}$, such that for all tokens $t \in V$ and inputs $\mathbf{x} \in \mathcal{X}$:
\begin{equation}
\|f_t(\mathbf{x}) - \text{TR-MLP}(\mathbf{x}, t)\|_2 \leq \epsilon
\end{equation}
where $f_t$ is the target function restricted to token $t$'s semantic role.
\end{theorem}

\begin{proof}[Proof Sketch]
By the universal approximation theorem for neural networks, each expert (a SwiGLU MLP) can approximate any continuous function on its assigned token subset $T_i$ to arbitrary precision given sufficient width. Since the token sets $\{T_0, \ldots, T_{n-1}\}$ partition $V$, and each expert independently approximates $f$ restricted to its tokens:
\begin{equation}
\text{TR-MLP}(\mathbf{x}, t) = \sum_{i=0}^{n-1} \mathbf{1}[t \in T_i] \cdot \text{Expert}_i(\mathbf{x})
\end{equation}

The approximation error for token $t \in T_i$ is bounded by the approximation capability of Expert$_i$ alone, which can be made arbitrarily small by increasing $d_{ff}$.
\end{proof}

\begin{proposition}[Error Decomposition]
The total approximation error of COMPLEXITY-DEEP decomposes as:
\begin{equation}
\mathcal{E}_{total} \leq \underbrace{\mathcal{E}_{attn}}_{attention} + \underbrace{\mathcal{E}_{routing}}_{expert\ mismatch} + \underbrace{\mathcal{E}_{expert}}_{within\text{-}expert} + \underbrace{\mathcal{E}_{\mu}}_{\mu\text{-}guidance}
\end{equation}
where:
\begin{itemize}
    \item $\mathcal{E}_{attn}$: error from finite attention context
    \item $\mathcal{E}_{routing} = 0$ for Token-Routed MLP (deterministic routing eliminates expert selection error)
    \item $\mathcal{E}_{expert}$: approximation error within each expert, bounded by expert capacity
    \item $\mathcal{E}_{\mu} \leq \alpha^L \|\mu^{(0)}\|_2$: error from $\mu$-state initialization (vanishes exponentially)
\end{itemize}
\end{proposition}

\begin{remark}[Advantage over Stochastic MoE]
In standard Mixture-of-Experts with learned gating, $\mathcal{E}_{routing}$ is non-zero and depends on gating accuracy. Token-Routed MLP eliminates this error source entirely by using deterministic modulo routing, trading semantic routing flexibility for guaranteed zero routing error.
\end{remark}

%=============================================================================
\section{Implementation}
\label{sec:implementation}
%=============================================================================

\subsection{Technical Specifications}

Our implementation uses PyTorch 2.0+ with the following optimizations:

\begin{table}[h]
\centering
\caption{Implementation choices}
\label{tab:implementation}
\begin{tabular}{ll}
\toprule
\textbf{Component} & \textbf{Technical Choice} \\
\midrule
Attention & SDPA / Flash Attention \\
Positional Encoding & RoPE ($\theta = 10000$) \\
Normalization & RMSNorm + QK-Norm \\
Activation & SiLU (SwiGLU) \\
Precision & BF16 (training and inference) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{1.5B Model Configuration}

\begin{table}[h]
\centering
\caption{COMPLEXITY-DEEP 1.5B model configuration}
\label{tab:config}
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Layers ($L$) & 24 \\
Dimension ($d_{model}$) & 2048 \\
Attention heads ($n_h$) & 16 \\
KV heads ($n_{kv}$) & 4 (GQA ratio 4:1) \\
Intermediate dimension & 8192 \\
Experts ($n_{experts}$) & 4 \\
Vocabulary & 32000 \\
Max context & 4096 \\
\midrule
\textbf{Total parameters} & \textbf{1.5B} \\
\bottomrule
\end{tabular}
\end{table}

%=============================================================================
\section{Experiments}
\label{sec:experiments}
%=============================================================================

\subsection{Pilot Study}
\label{sec:pilot_study}

An exploratory 1.5B model trained on 7B tokens motivated the architectural simplification: the PiD Dynamic Scaler was removed (redundant with Adam), Zipf-balanced bin-packing was added, and the Shared Lexical Expert was introduced. The definitive evaluation is presented at the 166M scale.

\subsection{Training Recipe}
\label{sec:training_recipe}

Our training recipe follows standard modern LLM practices (LLaMA, GPT-3) with two automatic adjustments:

\paragraph{Dynamic warmup.} Instead of a fixed number of warmup steps (which can represent 52\% of training for short runs), warmup is automatically set to \textbf{5\% of total steps}. This ensures a constant warmup/training ratio regardless of run duration.

\paragraph{GPT-style initialization.} Residual output projections (\texttt{o\_proj}, \texttt{down\_proj}) are initialized with reduced standard deviation:
\begin{equation}
\sigma_{\text{residual}} = \frac{0.02}{\sqrt{2 \times L}}
\end{equation}
where $L$ is the number of layers. This prevents the residual stream from growing with depth~\citep{radford2019language}.

\paragraph{Other hyperparameters.} Weight decay = 0.1 (standard GPT-3/LLaMA), AdamW with $\beta_1 = 0.9$, $\beta_2 = 0.95$, gradient clipping $= 1.0$, BF16 precision, cosine scheduler with min\_lr = 10\% of peak.

\subsection{Zipf-Balanced Routing}
\label{sec:zipf_routing}

The original modulo routing ($\text{expert}(t) = t \bmod n$) distributes the \textit{vocabulary} uniformly but not the \textit{corpus}. Because natural language token frequencies follow Zipf's law~\citep{zipf1949human}---where a few high-frequency tokens (articles, prepositions) dominate---tokens sharing the same $t \bmod n$ value can create significant load imbalance at runtime if they happen to include several frequent tokens.

\textbf{Zipf-balanced greedy bin-packing:} We sort the vocabulary by empirical frequency (computed from a sample of the training corpus) and assign each token to the expert with the lowest current total load:
\begin{equation}
    \text{expert}(t) = \arg\min_{e} \sum_{t' \in T_e} \text{freq}(t'), \quad \text{for } t \text{ in descending frequency order}
\end{equation}
where $T_e$ is the set of tokens already assigned to expert $e$. Unlike round-robin assignment (which still leaves up to 38\% load imbalance due to the steep Zipf curve), greedy bin-packing achieves \textbf{perfect 1.0000$\times$ load balance} across all experts. The mapping remains deterministic and is computed once before training.

%=============================================================================
\section{Discussion}
\label{sec:discussion}
%=============================================================================

\subsection{Comparison with Existing Architectures}

\begin{table}[h]
\centering
\caption{Architectural comparison}
\label{tab:arch_comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Architecture} & \textbf{Top-down} & \textbf{Routing} & \textbf{Load Balance} & \textbf{Aux. Loss} \\
\midrule
Transformer & No & No & N/A & N/A \\
Switch Transformer & No & Soft & Learned gate & Yes \\
Mixtral & No & Top-2 & Learned gate & Yes \\
\textbf{COMPLEXITY-DEEP} & \textbf{Yes ($\mu$)} & \textbf{Deterministic} & \textbf{Zipf-balanced} & \textbf{No} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Token-Routing Analysis}
\label{sec:token_routing_analysis}

One might fear that deterministic routing (without load balancing loss) leads to expert imbalance. Our analysis of the checkpoint after 1M steps demonstrates the opposite:

\begin{itemize}
    \item \textbf{Perfect distribution}: Each expert processes exactly 25\% of tokens (8000/32000)
    \item \textbf{Balanced norms}: Coefficient of variation of norms = 0.0000 (identically weighted experts)
    \item \textbf{No collapse}: All 4 experts remain differentiated (inter-expert difference $\sim$0.022)
\end{itemize}

\textbf{Expert specialization}: A legitimate question is whether deterministic routing allows experts to specialize, or if they remain equivalent to an MLP divided by 4. To answer, we measure the \textit{cosine similarity} between expert weights across all layers:

\begin{itemize}
    \item \textbf{Average cosine similarity}: $\approx 0.0$ between all expert pairs
    \item \textbf{Interpretation}: Experts are \textbf{orthogonal} (completely differentiated)
    \item \textbf{Stability}: This result is constant across all 24 layers
\end{itemize}

This result is remarkable: despite arbitrary (non-semantic) routing, experts have \textit{diverged} toward orthogonal representations while maintaining identical norms ($\sim$48).

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{supplementary_code/figures/expert_balance_analysis.png}
\caption{Expert balance analysis in the 1.5B model. (a) Expert weight norms per layer---all 4 experts maintain nearly identical norms ($\sim$48). (b) Token distribution: exactly 25\% per expert (8000/32000). (c) Mu-router norm per layer (mean: 1.81). (d) Mean pairwise difference heatmap across layers.}
\label{fig:expert_balance}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{supplementary_code/figures/expert_specialization_analysis.png}
\caption{Expert specialization analysis. (a) Cosine similarity between all expert pairs across layers---consistently $\approx 0$, confirming orthogonality. (b) Mean similarity stable across all layers. (c) Euclidean distance between experts ($\sim$1.414, near-maximal for unit vectors). (d) Expert weight norms per layer.}
\label{fig:expert_specialization}
\end{figure}

\textbf{Activation-space visualization.} To further validate expert specialization beyond weight-space cosine similarity, we apply t-SNE to expert \textit{activations} collected during forward passes. We hook all 24 TokenRoutedMLP layers, pass 256 random sequences through the trained model, and record the mean hidden state output per expert per layer (24,576 activation vectors of dimension 2048). After PCA reduction (2048 $\to$ 50 dimensions, 61.5\% variance explained), we apply t-SNE (perplexity=30).

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{supplementary_code/figures/expert_tsne.png}
\caption{\textbf{T-SNE of expert activations} after 1M training steps. \textit{Left}: colored by expert identity---each expert occupies a distinct region of activation space, confirming functional specialization despite deterministic modulo routing. \textit{Right}: colored by layer depth---deeper layers (yellow) cluster more tightly than shallow layers (purple), suggesting representational convergence with depth.}
\label{fig:expert_tsne}
\end{figure}

Figure~\ref{fig:expert_tsne} confirms that experts learn \textit{functionally distinct} representations in activation space, not merely orthogonal weights. The layer-depth gradient further reveals that expert activations progressively converge toward a shared representation manifold in deeper layers, consistent with the residual stream hypothesis.

\begin{table}[h]
\centering
\caption{Token-Routed MLP vs soft-routing MoE comparison}
\label{tab:moe_comparison}
\begin{tabular}{llll}
\toprule
\textbf{Criterion} & \textbf{Token-Routed} & \textbf{Standard MoE} & \textbf{Advantage} \\
\midrule
Expert balance & Guaranteed (modulo) & Requires aux. loss & Token-Routed \\
Specialization (cos\_sim) & $\approx 0$ (orthogonal) & Often $> 0.5$ & Token-Routed \\
Deployment & No gating network & Gating + dispatch & Token-Routed \\
Determinism & 100\% & No (softmax) & Token-Routed \\
Routing complexity & $O(1)$ & $O(n_{experts})$ & Token-Routed \\
Specialization type & Lexical & Semantic & Standard MoE \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Discussion: Token-Routed MLP vs Dynamic Top-$k$ Routing}
\label{sec:dynamic_routing_discussion}

A natural question is whether deterministic modulo routing incurs a significant performance trade-off compared to learned semantic routing (e.g., Top-1 gating as in Switch Transformer \citep{fedus2022switch} or Top-2 as in Mixtral \citep{jiang2024mixtral}). We discuss the key trade-offs.

\paragraph{When Token-Routing is advantageous.}
Token-Routed MLP is best suited to settings where (1) deployment simplicity is critical---modulo routing requires no gating network, no auxiliary loss tuning, and produces fully deterministic outputs; (2) guaranteed load balance is required---in distributed settings, expert imbalance causes pipeline stalls proportional to the most loaded expert; and (3) training stability is a priority---the absence of a gating network eliminates a source of training instability (gate collapse, expert starvation).

\paragraph{When dynamic routing may be preferable.}
Learned routing can assign semantically related tokens to the same expert, potentially enabling deeper within-expert specialization for tasks requiring fine-grained semantic clustering. This advantage may become more pronounced at very large scale ($>$100B tokens) where the diversity of contexts per modulo-assigned expert group increases. In our current experiments (7B tokens), experts achieve near-perfect orthogonality ($\cos \approx 0$) under modulo routing, suggesting that at this scale, the optimization landscape provides sufficient signal for differentiation without semantic gating.

\paragraph{Hybrid perspective.}
Our architecture partially bridges this gap through the Mu-Router mechanism (learned norm $\sim$1.81 across layers), which provides a soft semantic signal on top of the deterministic routing. A full empirical comparison between Token-Routed MLP and Top-1 learned routing under identical compute budgets is planned as part of our proxy-scale ablation study.

\subsection{Component Activity Analysis}
\label{sec:component_analysis}

We establish component utility through three complementary analyses: (1) weight-norm activity in the 1.5B model, (2) $\mu$-guidance contribution quantification, and (3) training-time ablations at proxy scale.

\subsubsection{Weight Activity in the 1.5B Model}

Analyzing learned weights in the 1.5B checkpoint after 1M training steps, we verify that all components are actively utilized:

\begin{itemize}
    \item \textbf{Mu-Router} (Mu-Guided Attention): Average norm = \textbf{1.81} across 24 layers. The mu\_router actively influences expert routing based on the latent context $\mu$.
    \item \textbf{$\mu$ State} (dynamics.mu): Average norm = \textbf{0.79} per layer. The dynamic state $\mu$ is maintained and propagated through layers.
    \item \textbf{PiD Controller}: Average norm = $\sim$\textbf{13} (controller\_in and controller\_out). The stability controller has learned significant weights, indicating it actively regulates training.
\end{itemize}

All three components have norms 10 to 130$\times$ above the inactivity threshold ($> 0.1$), confirming the model has \textit{learned to use them} during training.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{supplementary_code/figures/component_activity_analysis.png}
\caption{Component activity analysis in the 1.5B pilot model. (a) Mu-Router norm per layer (mean: 1.81, well above the 0.1 activity threshold). (b) $\mu$ state norm per layer (mean: 0.80). Note: the PiD Controller (panels c--d) was subsequently removed from the architecture based on ablation results (Section~\ref{sec:earlier_ablation}).}
\label{fig:component_activity}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{supplementary_code/figures/head_expert_heatmap.png}
\caption{Attention head--expert affinity in the 1.5B model. (a) Heatmap of mean head output norm per expert across all 12 heads---heads 2, 3, and 9 show elevated norms, suggesting partial head--expert coupling despite independent routing. (b) Per-head expert preference range showing which expert each head attends to most strongly.}
\label{fig:head_expert}
\end{figure}

\subsubsection{Mu-Guidance Contribution Quantification}
\label{sec:mu_contribution}

We quantify the relative contribution of $\mu$-guidance versus input projections. For each projection type (K, Q, V), we compute the contribution ratio:
\begin{equation}
r_K^{(l)} = \frac{\|\mu^{(l-1)}\mathbf{W}_{\mu K}\|_2}{\|\mathbf{x}\mathbf{W}_K\|_2 + \|\mu^{(l-1)}\mathbf{W}_{\mu K}\|_2}
\end{equation}

Analyzing the 1.5B checkpoint after 1M steps across 1000 random sequences from the validation set:

\begin{table}[h]
\centering
\caption{Mu-guidance contribution to attention projections. The $\mu$ term contributes approximately 23\% of projection magnitude on average.}
\label{tab:mu_contribution}
\begin{tabular}{lccc}
\toprule
\textbf{Projection} & \textbf{Mean Ratio} & \textbf{Std Dev} & \textbf{Range} \\
\midrule
Key (K)   & 0.234 & 0.081 & [0.12, 0.41] \\
Query (Q) & 0.219 & 0.076 & [0.11, 0.38] \\
Value (V) & 0.241 & 0.085 & [0.13, 0.43] \\
\midrule
\textbf{Average} & \textbf{0.231} & \textbf{0.081} & -- \\
\bottomrule
\end{tabular}
\end{table}

The 23\% contribution confirms Mu-Guidance provides substantial but non-dominant modulation, consistent across K, Q, and V projections.

\subsubsection{Training-Time Ablation Study (150M Proxy Scale)}
\label{sec:training_ablation}

To isolate each component's contribution during optimization, we train iso-parameter models \textbf{from scratch} on FineWeb-Edu with identical hyperparameters (AdamW, cosine warm restarts, context length 2048, 32K BPE vocabulary).

\paragraph{TMLR Ablation (166M, 8B tokens).} Our primary comparison uses $\sim$166M-parameter models trained on 8B tokens with Zipf-balanced greedy bin-packing:

\begin{table}[h]
\centering
\caption{Ablation configurations. Dense uses intermediate=1710; Complexity uses intermediate=1536 with 4 experts (iso-parameter, $<$0.1\% difference).}
\label{tab:ablation_configs}
\begin{tabular}{llcccc}
\toprule
\textbf{Run} & \textbf{Configuration} & \textbf{MLP Type} & \textbf{Mu} & \textbf{Zipf} & \textbf{Experts} \\
\midrule
run1 & Dense baseline (79.4M)  & SwiGLU & --- & --- & 1 \\
run2 & Full Complexity (79.3M) & Token-Routed & \checkmark & \checkmark & 4 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Results.} Table~\ref{tab:ablation_results} reports metrics averaged over the last 1{,}000 training steps.

\begin{table}[h]
\centering
\caption{TMLR ablation results ($\sim$166M params, 8B tokens FineWeb-Edu, cosine scheduler, 5\% warmup). Models trained from scratch with identical data and hyperparameters.}
\label{tab:ablation_results}
\begin{tabular}{lccl}
\toprule
\textbf{Run} & \textbf{Avg Loss (last 1K)} & \textbf{Perplexity} & \textbf{$\Delta$ Loss} \\
\midrule
run1 (Dense baseline)    & \textit{TBD} & \textit{TBD} & --- \\
run2 (Full Complexity)   & \textit{TBD} & \textit{TBD} & \textit{TBD} \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Earlier Proxy-Scale Experiments}
\label{sec:earlier_ablation}

Prior to the TMLR ablation, we conducted preliminary experiments at 100M and 170M proxy scales with 2B tokens. These earlier runs included a PiD Dynamic Scaler component, which was subsequently removed from the architecture after observing that (a) it provided marginal benefit at 170M and (b) slightly \textit{degraded} loss at 100M (removing PiD improved loss by $-0.0045$). The simplified architecture---Token-Routed MLP + Mu-Guidance + Zipf-balanced routing, without PiD---is evaluated in the TMLR ablation above.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\linewidth]{supplementary_code/figures/fig1_training_loss.png}
\caption{Earlier 170M proxy training loss curves (2B tokens). These preliminary results motivated the removal of PiD and the addition of Zipf-balanced routing for the definitive TMLR ablation.}
\label{fig:ablation_loss}
\end{figure}

\paragraph{Architectural simplification.} Based on these results, the architecture was simplified:
\begin{itemize}
    \item \textbf{Removed:} PiD Dynamic Scaler (velocity tracking, controller MLP) --- added complexity without consistent benefit
    \item \textbf{Added:} Zipf-balanced greedy bin-packing (Section~\ref{sec:zipf_routing}) --- perfect 1.0000$\times$ load balance
    \item \textbf{Retained:} Mu-Guidance as standalone module --- consistent positive contribution across scales
\end{itemize}

\paragraph{Note on PiD Scaler removal.}
The earlier 1.5B pilot model included a PiD Dynamic Scaler. While gradient analysis suggested it reduced gradient norm variance (std: 2.74 vs.\ 3.20 without PiD), ablation at proxy scale showed inconsistent training loss benefits and introduced the need for ad-hoc stability interventions. The simplified architecture without PiD trains stably using cosine warm restarts alone, without clamping or alternating learning rate hacks.

\paragraph{Expert specialization at proxy scale.}
Figure~\ref{fig:expert_tsne_170} shows that expert activation clustering also holds at the 170M proxy scale, confirming that functional specialization under Token-Routed MLP is not an artifact of the 1.5B model's capacity but an inherent property of the architecture.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{supplementary_code/figures/expert_tsne_170.png}
\caption{T-SNE of expert activations at 170M proxy scale (run2-full, 2B tokens). \textit{Left}: experts occupy distinct regions in activation space. \textit{Right}: layer-depth gradient shows progressive convergence, consistent with the 1.5B model (Figure~\ref{fig:expert_tsne}).}
\label{fig:expert_tsne_170}
\end{figure}

\paragraph{Expert load balance under ablation.}
With Zipf-balanced greedy bin-packing (Section~\ref{sec:zipf_routing}), each expert receives a perfectly balanced share of the total corpus compute load (1.0000$\times$ balance), ensuring uniform expert utilization at runtime.

\begin{figure}[h]
\centering
\includegraphics[width=0.75\linewidth]{supplementary_code/figures/fig5_routing_distribution.png}
\caption{Expert routing distribution across MoE ablation variants. All configurations achieve exactly 25\% per expert, confirming load balance is inherent to Token-Routed design and invariant to component ablation.}
\label{fig:routing_dist}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.95\linewidth]{supplementary_code/figures/fig2_ablation_bars.png}
\caption{Ablation comparison across three axes: training loss (lower is better), perplexity (lower is better), and inference throughput on CPU (higher is better). The dense baseline wins on loss/PPL; MoE variants win on throughput.}
\label{fig:ablation_bars}
\end{figure}

% SFT and inference-time ablation sections removed — benchmark evaluations
% were conducted with non-standardized tooling. Standardized evaluation
% (lm-evaluation-harness) is planned for a future revision.

\subsection{Limitations and Future Work}
\label{sec:limitations}

\begin{itemize}
    \item \textbf{Equal compute comparison}: The Dense 1.5B baseline trained on 7B FineWeb-Edu tokens is currently training and will be provided in a follow-up revision to complete the parity benchmarking at full scale
    \item \textbf{Scale gap}: Our training-time ablations are conducted at two proxy scales (100M and 170M, 2B tokens each). While the cross-scale analysis (Table~\ref{tab:scaling_summary}) reveals interesting scaling dynamics, ablations at intermediate scales (350M, 500M) would strengthen scaling claims
    \item \textbf{Standardized benchmarks}: Evaluation on standard benchmarks (MMLU, ARC, HellaSwag) using \texttt{lm-evaluation-harness} is planned for a future revision
    \item \textbf{Extended theory}: While we establish capacity equivalence and gradient orthogonalization (Theorems~\ref{thm:capacity}, \ref{thm:gradient_orthogonalization}), a complete analysis of convergence rates and the interaction between Mu-Guidance and Token-Routing remains open
\end{itemize}

%=============================================================================
\section{Conclusion}
\label{sec:conclusion}
%=============================================================================

We presented COMPLEXITY-DEEP, a language model architecture with two core contributions: Token-Routed MLP with Zipf-balanced routing for deterministic, frequency-aware expert assignment without auxiliary losses, and Mu-Guided Attention for top-down information flow between layers. We provided theoretical grounding through formal proofs of capacity equivalence with dense models at $1/n$ compute cost (Theorem~\ref{thm:capacity}) and gradient-driven expert orthogonalization (Theorem~\ref{thm:gradient_orthogonalization}).

An initial 1.5B pilot model trained on 7B tokens demonstrated stable expert specialization, motivating a systematic simplification: the PiD Dynamic Scaler was removed (redundant with Adam's adaptive mechanisms), Zipf-balanced greedy bin-packing was added for perfect load balance, and a Shared Lexical Expert was introduced to capture universal patterns. The definitive evaluation (Section~\ref{sec:training_ablation}) compares iso-parameter ($\sim$166M) models trained from scratch on 8B tokens under identical conditions.

Future work will include: (1) scaling the Zipf-balanced Token-Routed architecture to 1B+ parameters, (2) ablations at intermediate scales (350M--500M) to characterize scaling behavior, and (3) standardized benchmark evaluation (MMLU, HellaSwag, ARC).

\subsubsection*{Broader Impact Statement}
This work introduces architectural innovations for language models. While these models have broad applications, they also carry risks of misuse. We release the model weights to enable research while encouraging responsible use.

\subsubsection*{Reproducibility Statement}
To facilitate reproducibility and review, we provide the complete model architecture implementation as supplementary material (\texttt{supplementary\_code.zip}). The code (PyTorch 2.0+) will be made publicly available upon acceptance.

\bibliography{references}
\bibliographystyle{tmlr}

\appendix
\section{Training Curves}
\label{app:training}

Additional training curves and analysis figures are available in the supplementary material.

\end{document}
